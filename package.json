{
  "name": "explainable-mahjong-simulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "benchmark": "node scripts/benchmark.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
